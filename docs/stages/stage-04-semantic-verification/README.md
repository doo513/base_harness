# Stage 04 — Semantic Verification

Status: **IN PROGRESS — v0.5.0-rc2**.

The frozen contract is `CONTRACT.md`.

Current candidate adds verifier level/coverage normalization, Domain Profile verification contracts, content-address evidence integrity checks, a narrow structured artifact assertion verifier, and a reserved generic assertion namespace.

Promotion still requires final CI, adversarial probe, complete regression, and logic/implementation/structure re-review.
