# Stage 04 — Semantic Verification

Status: **IN PROGRESS — v0.5.0-rc1**.

The Stage 04 contract is frozen in `CONTRACT.md` before promotion.

Implementation target:

```text
Verifier implementation declaration
        ↓
normalized level + coverage
        ↓
Domain VerificationContract
        ↓
coverage / strength / evidence assessment
        ↓
Kernel-owned state commit or fail-closed rejection
```

Promotion to PASS requires the dedicated adversarial matrix, full regression, direct semantic probe, manifest fingerprint verification, and final logic/implementation/structure re-review.
