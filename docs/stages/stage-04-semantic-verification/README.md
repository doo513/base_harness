# Stage 04 — Semantic Verification

Status: **IN PROGRESS — v0.5.0-rc3**.

The frozen contract is `CONTRACT.md`.

`rc3` retains the rc2 semantic/evidence hardening and adds fail-closed harness-version matching on resume so the Stage 03 reproducibility gate remains valid across the Stage 04 release boundary.

Promotion still requires final CI, adversarial probe, complete regression, and logic/implementation/structure re-review.
